# dependencies
node_modules/

# build output (produced by the deploy workflow)
dist/

# local Claude Code settings (per-machine permissions)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
*.log

# raw downloaded gallery candidate images (rebuildable cache); keep gallery.json
scripts/gallery-cache/**/*-src.*
